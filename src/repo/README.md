# Repo Layer

Data access layer for persistent state (if needed).

Currently unused - Minesweeper is memory-only, but the layer exists for extensibility.

Contains:
- Repository interfaces
- Data persistence logic
